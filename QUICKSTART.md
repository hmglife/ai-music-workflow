# 快速上手 · Quick start

> 给音频策划的 5 分钟上手指南。详细说明见 `USAGE.md`，版本历史见 `CHANGELOG.md`。

这个工作流**靠对话触发**，全程说人话就行，不用记命令、不用配环境。
下面每一步给的都是"对它说这句话"。

---

## 第 0 步 · 准备（一次，约 1 分钟）

把整个 `ai-music-workflow` 文件夹放进 WorkBuddy 的技能目录：

- 个人用：`~/.workbuddy/skills/`
- 团队共享：`<项目>/.workbuddy/skills/`

**就这一步，没有别的。** 不用装 Python、不用敲 `pip`——运行环境 WorkBuddy 自带，
少什么组件工作流会自己补上，不会来烦你。

---

## 第 1 步 · 让它先认识你（一次性，但值得做）

### 1.1 蒸馏画像

不做这步也能用，但产出会很"通用"。做完之后，它写出来的东西才像**你**写的。

1. 把你过往写过的需求文档丢进 `data/inbox/`（Word / Excel / txt / Markdown 都行，越多越准）。
2. 对它说：

   > **"开始蒸馏"** 或 **"学习我的风格"**

3. 它会提炼出你的写作语气、审美倾向、各音乐类型的惯例、交付习惯，然后**让你确认和修正**。

没有现成文档？`examples/sample_briefs/` 里有脱敏示例可以先体验一遍流程。

> 画像存在技能文件夹**外面**（`~/.workbuddy/ai-music-workflow/profile.md`），
> 所以以后升级技能不会把它弄丢。换机器了想接过来，直接说"我旧画像在 X 路径，帮我接过来"。

### 1.2 换成你团队的模板（强烈建议）

自带的三个模板是**某一个团队**的版式（字体、行高都是那个团队的）。
你应该用**你自己的真实文档**做模板底稿，这样以后每一份产出都长得和你手写的一样。

把一份你写过的真实需求文档给它，然后说：

> **"用这份文档做成我的 CG 模板"**

字体、行高、列宽、边框会原样保留，只把内容清空换成占位符。自带模板按类型分三份：

| 模板 | 用途 |
|---|---|
| `CG.xlsx` | CG / 过场（带逐段时间轴对齐区） |
| `Lobby_Story_Login.xlsx` | 大厅 / 剧情 / 登录 共用 |
| `Feedback.xlsx` | 给音乐公司的修改反馈 |

---

## 第 2 步 · 把想法变成需求文档（日常主要用这个）

直接说你要什么，说不全没关系——它会按你的画像**有针对性地追问**，不会问卷式盘问。

> **"帮我写一份 Lobby 音乐需求文档"**
> **"把这个需求整理成 Excel"**

**可以顺手把素材一起给它**：概念图、参考录屏、临时参考曲、哼唱录音都行。
图片会嵌进文档，**所有素材都会被归档进交付文件夹**，随文档一起交出去。

产出不是一个孤零零的 Excel，而是一个**能整包发给音乐公司的文件夹**：

```
output/项目_类型_日期/
├── 项目_类型_日期.xlsx   需求文档（字体排版 = 你的模板）
├── Image/                概念图、关键视觉
├── Video/                参考录屏
├── Audio/                临时参考曲、哼唱
└── ASSETS.md             每个素材是什么、为什么重要
```

只创建实际用到的目录。素材是**复制**过去的，你的原件不动。

> 只要需求文档？到这里就停，它不会硬往下走。

---

## 第 3 步 · 往下走（按**谁来做这首曲子**选一条）

### 路线 A · 交给 AI 生成 → 要提示词

> **"把这个需求转成 Suno 提示词"**
> **"生成 Lyria 提示词"**

它会严格按该平台**官方文档**的规则产出可直接粘贴的内容，并给调优建议。
听完实际效果后把感受告诉它（"太商业""不够压迫""结构乱了"），它会诊断并改提示词。

两个当下最容易踩的坑，它会提醒你，这里先剧透：

- **Suno（v6）**：生成前把 **Variety 滑杆拉到 0**。它默认开着，会**自动改写你的 Style 提示词**——
  这是"v6 怎么不听话"的头号原因。另外否定词要写在 **Exclude Styles** 框，写进 Style 框反而变成"要"。
- **Lyria（3.5）**：CG 卡点用官方时间戳语法 `[0:00 - 0:10] Intro: ...`，和需求文档的时间轴行一一对应。
  注意它输出 44.1kHz、无分轨、带 SynthID 水印，**只能当 demo，不能直接当交付**。

### 路线 B · 交给音乐制作公司 → 要反馈文档

公司把成品发回来、你听完想提修改意见时走这条：

> **"我听了音乐公司做的成品，帮我写反馈"**

可以把公司交付的音频和截图一起给它。它会按你的专业表达习惯写成结构化、可执行的
**反馈文档（Excel）**，风格和需求文档一致，并自动适配两种模式：

- 你按时间点给意见（0-9s、副歌…）→ 按时间轴逐段成文（适合 CG 卡点）
- 你只说整体感受 → 总体印象 + 备注（适合 Lobby / Story）

多轮修改按版本递增（v1 → v2），**把 v1 一起归档**，评 v2 时才有对照。

---

## 三个最常问的

**Q：必须从第 1 步开始吗？**
不用。三个阶段相互独立，你需要哪一步就直接说哪一步。只是没有画像时产出会偏通用。

**Q：要 Word 或 PPT 版本行吗？**
行，直接说。内容走同一套流程，只是换个格式输出。但发给音乐公司还是建议用 Excel——
版式模板只有 Excel 版。

**Q：升级技能，我的画像和积累会丢吗？**
不会。画像存在技能文件夹外面。真要从旧版接，直接告诉它旧文件夹路径就行，
文件夹、单个 `profile.md`、甚至 zip 备份都认。
