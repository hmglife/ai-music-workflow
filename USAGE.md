# AI Music Workflow — 使用说明

> **版本 1.0.0** ｜ 第一次用请先看 **`QUICKSTART.md`**（5 分钟上手）｜ 改动历史见 `CHANGELOG.md`

一套给**音频策划**用的音乐创作工作流。它能学习你的工作习惯与审美，把一个想法变成专业需求文档，
再转成各 AI 音乐工具的精准提示词。三个阶段相互独立，你需要哪一步就做哪一步。

> 本文是**完整说明**；只想快速跑起来的话，`QUICKSTART.md` 更短。

> 这是一个 WorkBuddy Skill。把整个文件夹放进 WorkBuddy 的技能目录即可使用，然后在对话里用自然
> 语言触发（例如"帮我学习我的需求风格""帮我写一份 Lobby 音乐需求文档""把这个需求转成 Suno 提示词"）。

---

## 准备：你不需要装任何东西
把文件夹放进技能目录就能用。**不用装 Python、不用敲 `pip`**——运行环境 WorkBuddy 自带，
工作流会在需要时自己把缺的组件补上，不会让你去处理这些。

下面这张表只是让你知道背后用了什么、以及万一装不上会损失什么（比如公司网络不通时）：

| 组件 | 什么时候才需要 | 装不上的后果 |
|---|---|---|
| `openpyxl` | 生成 / 读取 Excel（几乎所有操作） | Excel 相关功能不可用 |
| `Pillow` | 只在**往 Excel 里嵌参考图**时 | 文档照常生成，只是图没嵌进去（素材仍在交付夹 `Image/` 里） |
| `python-docx` | 只在蒸馏时**读 Word 格式的旧文档** | 旧文档是 Excel/txt 的话完全用不到 |

素材归档、画像管理、依赖安装本身都是**纯标准库**，零依赖、永远可用。

---

## 阶段一 · 蒸馏画像（第一次使用时做一次）
让工作流学习"你是怎么写需求的"。
1. 把你过往写过的需求文档（Word / Excel / txt / Markdown）放进 `data/inbox/`。越多越准。
2. 在对话里说"开始蒸馏 / 学习我的风格"。
3. 工作流会提炼出你的写作语气、审美倾向、各类型（CG/Lobby/Story…）惯例、交付习惯，
   生成一份画像并让你确认、修正。
- 想重新学习：说"重新蒸馏 / 更新画像"，会覆盖更新（旧版自动备份）。
- 没有现成文档？`examples/sample_briefs/` 里有脱敏示例可先体验。

### 画像存在哪？升级技能会丢吗？
**不会。** 画像存在技能文件夹**外面**的固定位置：

```
~/.workbuddy/ai-music-workflow/profile.md
```

升级技能只替换技能文件夹，碰不到这里。画像是唯一无法重造的东西（它来自你的真实旧文档），
所以刻意放在外面。

### 从旧版把画像接过来（换机器 / 升级后）
**你不需要敲任何命令，直接说就行**，例如：

> "我之前的画像在 D:\旧备份\ai-music-workflow，帮我接过来"

工作流会自己完成导入，然后把画像内容摘要给你确认是不是那一份。

给什么形式都行：旧技能文件夹、里面的 `data` 目录、解压出来的备份、单独一个 `profile.md`、
甚至旧技能的 zip 包。路径带空格或引号都能处理；如果只剩带时间戳的备份文件，会自动取最新那份。

覆盖前一律自动备份，不会把现有画像弄丢。

> 如果工作流发现你还没有画像，它会**先问你**有没有旧的可以导入，而不是直接让你重新蒸馏——
> 重新蒸馏需要你再找一遍旧文档，能省就省。

## 阶段二 · 生成需求文档（Excel）
把一句想法变成结构统一、排版专业的 Excel 需求文档。
1. 描述需求（项目、场景、情绪、参考、时长、结构、是否循环、交付格式、截止时间…）。说不全没关系，
   工作流会按你的画像有针对性地追问。
2. **可附任何参考素材**：概念图、参考视频、临时参考曲、哼唱录音都行。图片会被嵌进文档（和你手写的
   表一样）；**所有素材都会被归档进交付文件夹**，随文档一起交出去。视频内容读不了，若音乐意图取决于
   画面上发生了什么，请另给一张截图——但视频原件照样会归档。
3. 选模板（`data/templates/` 下，按音乐类型）：
   - `CG.xlsx` — CG / 过场（含逐段时间轴对齐区）
   - `Lobby_Story_Login.xlsx` — Lobby / Story / Login 共用
4. 工作流润色需求并生成 Excel——**字体与排版完整保留你团队的格式**。
5. **生成的是一个自包含交付文件夹**，放在你当前的工作空间里（不是技能目录）：

```
output/项目_类型_日期/
├── 项目_类型_日期.xlsx   需求文档
├── Image/                概念图、关键视觉、视频截图
├── Video/                参考录屏 / 参考片段
├── Audio/                临时参考曲、哼唱录音、成品音频
├── Other/                pdf、zip、工程文件…
└── ASSETS.md             素材清单：每个文件是什么、为什么重要
```

   只会创建实际用到的类型目录（只给一张图就只有 `Image/`）。素材是**复制**过去的，你的原件不动。
   文档里会用相对路径引用素材（如 `参考 Audio/temp_track.wav`），音乐公司拿到整个文件夹就能对齐。
   清单里**只写相对路径**，不会暴露你本机的目录结构。

> 只想要需求文档？到这里就停，不会硬往下生成提示词。

### 想要 Word 或 PPT 版本？
可以，直接说就行（例如"这份也给我一份 Word 的""做成 PPT 好去过评审"）。内容走同一套流程
（同样按你的画像润色、同样归档素材），只是最后换个格式输出，文件放进同一个交付文件夹。

两点你需要知道：
- **发给音乐公司还是建议用 Excel**，那是他们现在收的格式，也只有 Excel 有你团队的版式模板。
- 你要是有自己的 Word / PPT 模板，也能直接拿来填——**在模板里留占位符**（例如 `<REQUIREMENT>`），
  工作流替换占位符时会完整继承你模板的字体、字号、颜色；直接往末尾追加则不会继承格式。

### 想用自己的模板 / 换成你团队的格式
把一份真实文档给它，说"用这份文档做成我的 CG 模板"即可（字体、行高、列宽、边框全部保留，
只清空内容）。它内部执行的是：
```
python scripts/make_template.py --source 你的真实文档.xlsx --out data/templates/类型名.xlsx --clear-values
```

## 阶段三 · 把需求往下走（两条分支）
按**谁来制作音乐**选分支：

### 分支 A · 交给 AI → 生成提示词
把需求转成某个 AI 音乐工具的精准提示词，并能按你的反馈不断调优。
1. 选平台。内置适配器：
   - `adapters/suno.md` — Suno **v6 / v6-wild / v6-mini**（三字段：Style + Exclude Styles + Lyrics，
     外加必须先设的 Variety 等滑杆）
   - `adapters/lyria.md` — Lyria **3.5**（单条自然语言描述，支持 `[0:00 - 0:10]` 时间戳卡点）
2. 工作流严格按该平台**官方文档**的规则生成提示词，给出可直接复制的内容 + 调优建议。
3. **反馈调优**：把实际生成效果告诉它（如"太商业""不够压迫""结构乱了"），它会诊断、改提示词，
   并把有价值的经验写回适配器（`data/feedback/<平台>.md`），让工作流越用越懂你。

#### 接入一个新的 AI 音乐工具（包括未来出现的）
1. 复制 `adapters/_TEMPLATE.md` 为 `adapters/<工具名>.md`。
2. 找到该工具的**官方提示词文档**（联网查，或由你提供官方资料）。
3. 按模板把规则填进去（官方来源 + 版本 + 核对日期 + 写法 + 示例 + 排错表）。
   **一切以官方文档为准，不臆测。** 官方更新了，适配器也随之更新。

### 分支 B · 交给音乐制作公司 → 写反馈文档
当需求不是给 AI、而是发给专业音乐制作公司制作，你听了成品后需要写修改反馈时走这条。
1. 告诉它你听后的感受（哪里不对、想要什么效果），可附参考截图，也可以把公司交付的音频给它。
2. 它会按你的专业表达习惯，把反馈润色成结构化、可执行的**反馈文档（Excel）**，风格与需求文档一致。
3. **两种模式自适应**：
   - **逐段时间点**：你按时间点（如 0-9s、副歌）甚至附图给意见 → 按时间轴逐段成文（适合 CG 卡点）。
   - **整体反馈**：你只说整体感受 → 总体印象 + 备注（适合 Lobby / Story）。
4. 反馈文档同样是自包含交付文件夹（含 `Audio/` 里公司交付的那一版、截图等素材 + `ASSETS.md`），
   放在你的工作空间里。多轮修改时按版本递增（v1 → v2）——把 v1 一起归档，评 v2 时才有对照。

---

## 目录结构
```
ai-music-workflow/
├── SKILL.md            # 工作流主控（触发、路由、画像门控）
├── QUICKSTART.md       # 5 分钟快速上手（第一次用看这个）
├── USAGE.md            # 本说明
├── CHANGELOG.md        # 版本历史与升级说明（拿到新版先看这个）
├── references/         # 各阶段详细流程（含 3A 提示词 / 3B 反馈文档）
├── adapters/           # 各 AI 平台的提示词规则（suno / lyria / 新工具模板）
├── scripts/            # 读素材 / 造模板 / 生成 Excel（含嵌图、清占位）/ 归档素材 / 画像管理 / 依赖自动安装
├── examples/           # 脱敏示例文档（供参考，运行时不使用）
└── data/               # 技能自带资产
    ├── templates/      # Excel 模板（CG / Lobby_Story_Login / Feedback）
    ├── inbox/          # 放素材供蒸馏
    └── feedback/       # 各 AI 平台提示词调优记录（非音乐公司反馈）

你的画像不在这里，而在 ~/.workbuddy/ai-music-workflow/profile.md —— 升级技能不会丢。
```

> **生成的交付文件夹（文档 + 素材 + 清单）会放在你的工作空间里**，不会塞进技能目录——技能保持干净、
> 便于分享。核心的 Excel 需求 / 反馈文档由工作流自带的脚本产出（只用 openpyxl），**不经过任何外部
> 文档插件**，这样才保得住你团队的字体和行高；Word / PPT 等其他格式则借用 WorkBuddy 内置的本地
> Office 能力来输出。

> **一个容易被坑的细节**：Excel 里的参考图必须由工作流自己嵌入。内置通道插的是 Excel 365 的
> "单元格内图片"，在旧版 Excel、WPS 或邮件预览里会显示成 `#VALUE!`——需求文档是要发给音乐公司的，
> 这个不能冒险。Word / PPT 的图片没这个问题。

---

## 一句话总结
一次蒸馏，长期复用；需求自动成文，提示词对齐官方，反馈反哺工作流。
工具会过时，工作流不会——模型可控的那天，把它接上来即可。
